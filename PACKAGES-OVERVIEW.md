# Обзор всех пакетов и инструментов

Сводка того, что ставят `setup.sh`, `tools-extra.sh` и `tui-tools.sh`, одним
файлом и сгруппировано по смыслу — чтобы удобнее было решить, что оставить,
что убрать, что заменить. Это справочный файл для чтения/редактирования
списков в `lib/packages.sh` и `lib/tools_extra.sh`, сам он ни на что не
влияет и не выполняется.

Условные обозначения: **[core]** — `setup.sh` (базовая установка, диалог
по умолчанию ставит всё), **[extra]** — `tools-extra.sh` (опционально, по
одному через диалог/аргументы), **[tui]** — `tui-tools.sh` (только Linux,
администрирование сервера).

---

## 1. Пакетный менеджер-прослойка **[core]**

| ОС    | Что ставится                  |
| ----- | ----------------------------- |
| macOS | Homebrew                      |
| Linux | flatpak + репозиторий flathub |

Без этого не работает установка почти всего остального ниже.

## 2. Базовые системные пакеты **[core]**

| Пакет           | Назначение                                                             |
| --------------- | ---------------------------------------------------------------------- |
| git             | система контроля версий                                                |
| ssh (openssh)   | клиент/сервер SSH                                                      |
| stow            | раскладка dotfiles симлинками (для отдельного репозитория с конфигами) |
| htop            | монитор процессов                                                      |
| nvim            | редактор                                                               |
| tmux            | мультиплексор терминала                                                |
| zsh             | оболочка (+ становится дефолтной через `chsh`)                         |
| pass            | CLI-менеджер паролей на GPG                                            |
| gnupg           | шифрование/подпись, нужен для `pass`                                   |
| eza             | замена `ls` (см. §5 — это уже современный форк, не устаревший `exa`)   |
| wireguard-tools | утилиты WireGuard VPN                                                  |
| mc              | файловый менеджер (см. §5 — дублируется с vifm/yazi)                   |
| vifm            | файловый менеджер (см. §5 — дублируется с mc/yazi)                     |


## 3. Zsh/tmux-экосистема **[core]**

- **oh-my-zsh** + плагины `zsh-autosuggestions`, `zsh-syntax-highlighting`
- **oh-my-tmux** — готовый конфиг tmux
- **tpm** (менеджер плагинов tmux) + плагины: `tmux-sensible`, `tmux-resurrect`,
  `tmux-continuum`, `tmux-yank`, `tmux-thumbs`, `tmux-fzf`, `tmux-fzf-url`,
  `catppuccin-tmux` (тема), `tmux-sessionx`, `tmux-floax`

⚠️ **Возможное пересечение**: `tmux-sessionx` и `tmux-fzf` оба дают
fzf-подобный выбор/переключение сессий/окон — на практике достаточно одного
из них, но оба ставятся без вопроса.

## 4. Терминал и шрифты **[core]**

- **alacritty** + `alacritty-theme` (набор тем)
- **Nerd Fonts**: Hack, 0xProto, JetBrainsMono

## 5. Файловые менеджеры — 3 штуки на одну задачу

| Пакет    | Откуда  | Стиль                                                      | Заметка                               |
| -------- | ------- | ---------------------------------------------------------- | ------------------------------------- |
| **mc**   | [core]  | классический Norton Commander (2 панели, F-клавиши)        | самый привычный/старый                |
| **vifm** | [core]  | vim-раскладка клавиш, тоже 2 панели                        | альтернатива mc для vim-пользователей |
| **yazi** | [extra] | Rust, async, превью изображений/PDF/архивов, самый быстрый | самый современный вариант             |


Для полноценной работы **yazi** ставятся ещё три отдельных пункта (в
диалоге `tools-extra.sh` идут сразу за ним с отступом, как подпункты):

- **chafa** — вывод картинок прямо в терминал (нужен для превью изображений)
- **7zip** — распаковка архивов для превью содержимого `.zip`/`.7z` и т.п.
- **pdftoipe** — ⚠️ **вероятно, ошибка выбора пакета.** `pdftoipe`
  конвертирует PDF в XML-формат редактора чертежей *Ipe* — это не имеет
  отношения к просмотру PDF в yazi. Официальная зависимость yazi для
  превью PDF — `poppler-utils` (даёт `pdftoppm`, рендерит превью страницы
  как картинку). Стоит проверить `lib/tools_extra.sh:650` и, вероятно,
  заменить `pdftoipe` на `poppler-utils`.

**Рекомендация по группе**: если реально пользуетесь одним из трёх — двум
остальным можно оставить статус "по желанию" (снимать галочку в диалоге), а
не ставить все три по умолчанию каждый раз.

## 6. Мониторинг ресурсов — современные замены классики **[extra]**

| Устаревшее/классика | Современная замена (этот репозиторий) | Комментарий                                                                                                            |
| ------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `top` / `htop`      | **bottom** (бинарь `btm`)             | htop уже в core, bottom — доп. вариант с графиками                                                                     |
| `du`                | **dust**                              | наглядное дерево использования диска                                                                                   |
| `df`                | **duf**                               | наглядные точки монтирования                                                                                           |
| `ping`              | **gping**                             | тот же ping + график задержки в реальном времени                                                                       |
| `traceroute`/`mtr`  | **trippy** (бинарь `trip`)            | traceroute + ping в одном TUI                                                                                          |
| `nethogs`/`iftop`   | **bandwhich**                         | кто из процессов сколько трафика ест                                                                                   |
| `neofetch`          | **fastfetch**                         | neofetch официально archived/не поддерживается — fastfetch его прямая замена, уже так и указано в описании инструмента |
| `cat`               | **bat**                               | подсветка синтаксиса + git-diff; на Debian/Ubuntu бинарь `batcat`                                                      |

Все восемь — независимые опциональные пункты `tools-extra.sh`, ничего
menять не нужно, но при желании урезать список — это первые кандидаты
"нужно ли мне это второй раз, если htop/eza уже стоят".

## 7. Логи и отладка

| Пакет        | Откуда  | Фокус                                                         |
| ------------ | ------- | ------------------------------------------------------------- |
| **lnav**     | [extra] | просмотр/анализ файлов логов, SQL-запросы по логам            |
| **gonzo**    | [extra] | realtime-логи в k9s-стиле, нативная поддержка Kubernetes/OTLP |
| **tui-logs** | [tui]   | системные логи (journald + файлы), Linux-сервер               |

Три инструмента закрывают разные сценарии (файлы логов вообще / потоковые
структурированные логи и k8s / именно journald на сервере), пересечение
неполное — оставлять все три оправдано, если нужны все три сценария.

> `gonzo` оставить в `tools-extra.sh`, `lnav` переместить в `setup.sh`.

## 8. Git / Docker / Kubernetes TUI **[extra]**

- **lazygit** — TUI для git
- **lazydocker** — TUI для docker/docker-compose
- **k9s** — TUI для Kubernetes-кластера

Независимы друг от друга, пересечения нет — ставить по факту использования
соответствующей технологии.

> Пакеты `lazygit, lazydocker, k9s` все оставить в `tools-extra.sh`

## 9. SSH и сеть — сильное пересечение, кандидат на прополку **[extra]**

| Пакет        | Что делает                                                                           |
| ------------ | ------------------------------------------------------------------------------------ |
| **ssh-list** | TUI-менеджер SSH-подключений: добавление/сортировка/поиск, импорт из `~/.ssh/config` |
| **lazyssh**  | TUI для SSH-подключений в стиле lazydocker/k9s                                       |
| **sshs**     | быстрый fuzzy-выбор хоста из `~/.ssh/config` для подключения                         |
| **keyward**  | управление SSH-*ключами*, `~/.ssh/config`, аудит безопасности, шифрованные бэкапы    |

⚠️ **ssh-list, lazyssh и sshs решают практически одну и ту же задачу** —
быстрый выбор/подключение к хосту из `~/.ssh/config`. Разумно оставить один
(например, `sshs` — самый лёгкий, просто fuzzy-picker) и убрать два других
из `TOOLS_EXTRA_NAMES`, либо оставить все, если нравятся разные UI и это не
мешает. `keyward` не дублирует остальные — он про сами ключи и аудит, а не
про подключение.

> В `tools-extra.sh` оставить только `lazyssh, keyward`. Пакеты `ssh-list, sshs` убрать совсем.

Остальное сетевое:

- **vortix** — TUI для WireGuard/OpenVPN: телеметрия, kill switch, DNS/IPv6-утечки
- **wlctl** — TUI для wifi/ethernet/vpn через NetworkManager (только Linux)
- **termscp** — терминальный SCP/SFTP/FTP/S3-клиент (замена FileZilla/WinSCP в TUI)
- **gpg-tui** — управление ключами GnuPG (надстройка над `gnupg` из core)

> Пакеты оставить в `tools-extra.sh`: `vortix, termscp, gpg-tui, wlctl`.

## 10. Разное CLI/TUI **[extra]**

- **tldr** (пакет `tealdeer`) — короткие примеры вместо `man`
- **termusic** — терминальный музыкальный плеер
- **mangofetch** — TUI-загрузчик медиа (YouTube/torrent/SoundCloud/Instagram) поверх `yt-dlp`/`ffmpeg`
- **slumber** — терминальный REST/gRPC-клиент (замена Postman/Insomnia в TUI)
- **herdr** — агенто-осведомлённый мультиплексор терминала для coding-агентов (herdr.dev)

> Пакеты `tldr, termusic, mangofetch, slumber, herdr` пусть остаюстся в `tools-extra.sh`
## 11. Языки и инструменты разработки **[core]**

- **rust** (`rustup`) — нужен как фолбэк-сборщик для многих `[extra]`-пакетов без нативных пакетов (`cargo install`)
- **uv** — менеджер Python-пакетов/окружений (уже современная замена связки pip+venv+pyenv/poetry)
- **omp-manager** — TUI-мастер настройки Oh My Posh (тема шелла); требует cargo, ставится после rust

## 12. Neovim IDE — взаимоисключающий выбор **[extra] `./tools-extra.sh ide`**

- **AstroNvim**
- **NvChad**
- **LunarVim**
- пункт «очистить редактор» — сносит конфиги/данные/кэш всех трёх сразу

AstroNvim и NvChad физически ставятся в один и тот же `~/.config/nvim`,
поэтому одновременно может быть только один из двух; LunarVim независим
(`~/.config/lvim`). Это единственная группа, где выбор одного пункта явно
подразумевает "не остальные два" — уже реализовано диалогом, просто держите
в уме при редактировании.

## 13. tui-tools.sh — администрирование Linux-сервера **[tui]** (только Linux)

14 независимых бинарников с GitHub Releases (не через системный пакетный
менеджер), каждый — preview-confirm-run перед изменением системы:

| Инструмент     | Область                                                                                                       |
| -------------- | ------------------------------------------------------------------------------------------------------------- |
| tui-firewall   | ufw/firewalld/nftables                                                                                        |
| tui-systemd    | юниты systemd: что упало и почему                                                                             |
| tui-snapper    | снапшоты Btrfs/LVM                                                                                            |
| tui-network    | интерфейсы, маршруты, соединения                                                                              |
| tui-secure     | аудит защищённости сервера                                                                                    |
| tui-users      | пользователи и группы                                                                                         |
| tui-update     | обновления пакетов                                                                                            |
| tui-disk       | диски и точки монтирования                                                                                    |
| tui-ssh        | конфигурация и активные сессии SSH (серверная сторона — отличается от клиентских ssh-list/lazyssh/sshs из §9) |
| tui-logs       | системные логи (journald + файлы)                                                                             |
| tui-cron       | задания cron и systemd-таймеры                                                                                |
| tui-cert       | TLS-сертификаты                                                                                               |
| tui-containers | контейнеры Docker/Podman                                                                                      |
| tui-samba      | шары, учётки и live-подключения Samba                                                                         |

Это отдельное семейство от Homebrew/apt-мира — риск здесь не "лишний
пакет", а то, что `tui-firewall`/`tui-users`/`tui-cron`/`tui-secure` умеют
менять live-конфигурацию сервера, если согласиться с предложенной командой.

---

## Итог: где стоит подрезать список

1. **Файловые менеджеры** (§5): mc / vifm / yazi — три решения одной
   задачи. Если основной сценарий — yazi, mc/vifm можно перевести в
   необязательные.
2. **SSH-клиенты** (§9): ssh-list / lazyssh / sshs — оставить один.
3. **pdftoipe** у yazi (§5) — похоже на неверный пакет вместо
   `poppler-utils`, стоит проверить перед следующим прогоном на новой
   машине.
4. **tmux-sessionx vs tmux-fzf** (§3) — оба дают fzf-выбор сессий, можно
   оставить один.

Уже отмечено как устаревшее и сознательно исключено из установки (см.
`PACKAGES.md` → "Явно исключено"): Zed, musikcube.




# Что нужно изменить


